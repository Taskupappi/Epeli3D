#version 330 core

in vec2 texCoord;
in vec4 objectColor;
in vec3 normal;

out vec4 Color;

void main()
{
	//vec3 lightColor = vec3((GLfloat)(1/125), (GLfloat)(1/125), 0);
	float ambientStrength = 0.1f;
	//vec3 ambient = ambientStrength * lightColor;
	vec4 result = vec4(ambientStrength * objectColor.xyz, objectColor.);
    	//Color = vec4(result, 1.0f)
	Color = result;
}