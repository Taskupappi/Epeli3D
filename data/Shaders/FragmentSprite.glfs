#version 330 core

in vec4 objectColor;
in vec2 TexCoord;
out vec4 color;

uniform sampler2D sampler;
void main()
{
	color = texture2D(sampler,TexCoord,) + varyCol;
	//gl_FragColor = texture2D(sampler, UV) + varyCol;
}